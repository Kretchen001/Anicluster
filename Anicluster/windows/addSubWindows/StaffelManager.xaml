<Window x:Class="Anicluster.windows.StaffelManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Anicluster.windows"
        mc:Ignorable="d"
        Title="Staffel-Manager" Height="250" Width="600">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="10*" />
            <RowDefinition Height="5*" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="dataGridStaffeln" Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="dataGridCounter"  Width="3*" Binding="{Binding counter}" Header="Staffel-Nummer">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="dataGridEpisodes" Width="5*" Binding="{Binding episodes}" Header="Episodenanzahl"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" FontWeight="Bold" Content="Anzahl der Episoden" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <TextBox x:Name="textBoxGivenEpisodes" Grid.Row="1" Margin="10" TextChanged="textChangeEpisodes" />
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <Button Margin="15,0,15,0"  VerticalAlignment="Center" Content="Hinzufügen" Background="LightGreen" Click="clickAdd" />
            <Button Margin="15,0,15,0" Grid.Row="2" VerticalAlignment="Center" Content="Entfernen" Background="RosyBrown" Click="clickDelete"/>
        </Grid>
        
        <Button Margin="15,0,15,0" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Content="Fertig" Background="AliceBlue" Click="clickFinish"/>

    </Grid>
</Window>
