<Window x:Class="Anicluster.windows.TagList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Anicluster.windows"
        mc:Ignorable="d"
        Title="Tags - Liste" Height="450" Width="800">
    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="14*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="9*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <TextBox x:Name="textBoxFilter" Margin="8" Grid.Column="0" TextChanged="filterChanged" VerticalAlignment="Center"/>
            <Button x:Name="FilterLeeren" Margin="8" Grid.Column="1" Content="Leeren" Click="clickFilterReset"/>
        </Grid>
        <DataGrid Name="dataGridTagList" AutoGenerateColumns="False" Grid.Row="1" MouseDoubleClick="doubleMouseClickChangeCheck" CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" IsReadOnly="True" Binding="{Binding tagDesignator}" Width="20*"/>
                <DataGridCheckBoxColumn x:Name="dgCheckBoxColumnAddTag" Header="Ausgewählt" Binding="{Binding isChecked}" IsReadOnly="False" Width="4*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="2" Click="clickConfirmTagChoice" >Fertig</Button>
    </Grid>
</Window>
